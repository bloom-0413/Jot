package com.jot.app.behavior

enum class NewNoteKeyboard {
    ENABLED,
    DISABLED
}

enum class NoteSort {
    CREATED_AT,
    UPDATED_AT
}

enum class TrashAutoDelete {
    ENABLED,
    DISABLED
}

enum class CrashLog {
    ENABLED,
    DISABLED
}

enum class AutoUpdate {
    ENABLED,
    DISABLED
}
